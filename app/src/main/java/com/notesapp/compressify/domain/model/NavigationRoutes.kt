package com.notesapp.compressify.domain.model

enum class NavigationRoutes {
    HOME,
    COMPRESS_IMAGE,
    COMPRESS_VIDEO,
    INDIVIDUAL_IMAGE_PREVIEW,
    INDIVIDUAL_VIDEO_PREVIEW,
    LIBRARY
}